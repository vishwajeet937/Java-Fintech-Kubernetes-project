apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: banking

spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      nodeSelector:
        kubernetes.io/hostname: worker-node-1
      containers:
      - name: frontend
        image: vishwajeetkumar937/java-banking-frontend:1.0
        ports:
        - containerPort: 80

        env:
        - name: DB_URL
          value: "jdbc:postgresql://postgres-service.banking.svc.cluster.local:5432/bankingdb"
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: POSTGRES_PASSWORD
        - name: JWT_SECRET
          value: "1df0f6fe85156663c1c36121a92b3fc1702bcf52779752d63c25d75915bcd831"
        - name: JWT_EXPIRATION_MS
          value: "86400000"
        - name: CORS_ALLOWED_ORIGINS
          value: "*"

        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 50
          periodSeconds: 10